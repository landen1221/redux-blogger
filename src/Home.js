import BlogCard from "./BlogCard"

const Home = () => {
    return (
        <div className="Home">
            <p>Welcome to <b>Microblog,</b> our innovative site for communicating on the information superhighway.</p>

            {/* {blogs.map((blog) => {
                return <BlogCard />
            })} */}
        </div>
    )
}

export default Home